# Gradle Memory Settings for Minecraft Mod Building
# Increases heap size to prevent OutOfMemoryError during decompilation

org.gradle.jvmargs=-Xmx3G -Xms1G -XX:MaxPermSize=512m
org.gradle.daemon=false
